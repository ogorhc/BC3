import StringImporter from '../importers/StringImporter';
import { ParserModes } from '../types/ParserModes';

export default class BC3 {
  static parse(input: string, options?: { mode: ParserModes }) {
    const importer = new StringImporter();
    return;
  }
}
